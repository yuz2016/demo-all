<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.beforeall.mybatis.mapper.UserMapper">
    <select id="getUser" parameterType="int" resultType="com.example.beforeall.mybatis.model.User">
        select id,name from user where id = #{id}
    </select>
</mapper>